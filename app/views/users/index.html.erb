<ol>
    <% @users.each do |user|%>
      <li><%= user.first_name %>/<%= user.last_name %> ---<%= user.category %> --- <%= user.description %> ---<%= user.language %> </li>
      <%= link_to "show", user_path(user) %>
    <% end %>
</ol>